{
  "buildCommand": "cd ../.. && npm install && npm run build:shared && cd apps/hash-generator && npm run build",
  "installCommand": "cd ../.. && npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}