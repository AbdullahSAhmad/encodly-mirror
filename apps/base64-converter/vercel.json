{
  "buildCommand": "cd ../.. && npm install && npm run build:shared && cd apps/base64-converter && npm run build",
  "installCommand": "cd ../.. && npm install",
  "outputDirectory": "apps/base64-converter/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}