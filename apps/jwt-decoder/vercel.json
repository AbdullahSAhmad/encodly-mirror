{
  "buildCommand": "cd ../.. && npm install && npm run build:shared && cd apps/jwt-decoder && npm run build",
  "installCommand": "cd ../.. && npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}