{
  "buildCommand": "cd ../.. && npm install && npm run build:shared && cd apps/json-formatter && npm run build",
  "installCommand": "cd ../.. && npm install",
  "outputDirectory": "apps/json-formatter/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}