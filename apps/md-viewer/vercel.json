{
  "buildCommand": "cd ../.. && npm install && npm run build:shared && cd apps/md-viewer && npm run build",
  "installCommand": "cd ../.. && npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "redirects": [],
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}