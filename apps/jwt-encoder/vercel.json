{
  "buildCommand": "cd ../.. && npm install && npm run build:shared && cd apps/jwt-encoder && npm run build",
  "installCommand": "cd ../.. && npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "redirects": [],
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}